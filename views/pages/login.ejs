<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('../partials/head'); %>
    </head>
    <body class="container">
        <div class="border p-2 w-50">
            <form id="frmLogin" action="/login" method="POST">
                <h4>Đăng nhập</h4>
                <div class="form-group">
                    <label for="txtEmail">Email</label>
                    <input id="txtEmail" class="form-control" name="email" type="text" placeholder="Email"/>
                </div>
                <div class="form-group">
                    <label for="txtPassword">Email</label>
                    <input id="txtPassword" class="form-control" name="password" type="password" placeholder="Password"/>
                </div>
                <% if(typeof message !== 'undefined') {%>
                    <p class="alert alert-danger"><%=message%></p>
                <%}%>
                <input id="btnSubmit" class="btn btn-success" type="submit" value="Đăng nhập"/>
            </form>
        </div>
    </body>
</html>